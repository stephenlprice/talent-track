const connection = require('./connection.js');

// Object Relational Mapper (ORM)
// The ?? signs are for swapping out table or column names
// The ? signs are for swapping out other values

const view = (column, table) => {
    const query = 'SELECT'
};



const orm = {
    view
};

module.exports = orm;